stages:
  - test
  - lint

test:
  image: python:3.11
  script:
    - apt-get update && apt-get install -y libgl1
    - pip install .
    - pytest tests/

lint:
  image: python:3.11
  script:
    - pip install .
    - isort --check .
    - black --check .
